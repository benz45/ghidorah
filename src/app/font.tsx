import { Inter, Sansita_Swashed, Montserrat } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const sansita_swashed = Sansita_Swashed({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-sansita-swashed",
});

export const montserrat = Montserrat({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-montserrat",
});
